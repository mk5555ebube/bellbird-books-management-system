import { PlaceholderPage } from "@/components/layout/placeholder-page";

export const metadata = {
  title: "Customers",
};

export default function CustomersPage() {
  return (
    <PlaceholderPage
      title="Customers"
      description="Manage fictional customer records."
      message="Customer management is reserved for its assigned project epic."
    />
  );
}
