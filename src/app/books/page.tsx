import { PlaceholderPage } from "@/components/layout/placeholder-page";

export const metadata = {
  title: "Books",
};

export default function BooksPage() {
  return (
    <PlaceholderPage
      title="Book catalogue"
      description="Search and manage recorded book titles."
      message="The catalogue, search controls and filters will be implemented in their assigned stories."
    />
  );
}
